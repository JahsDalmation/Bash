#!/bin/bash

# A simple script to install Spotify (RPM Fusion repos must be enabled)
sudo dnf install lpf-spotify-client -y

# Run the newly installed package

# Additionally install EasyEffects for equalization options (Use # to comment out code)
sudo dnf install easyeffects -y
